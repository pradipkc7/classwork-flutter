import 'package:flutter/material.dart';

class SimpleIntrestScreen extends StatelessWidget {
  const SimpleIntrestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  SizedBox.expand(child: Center(child: Text('i am simple intrest screen')));
  }
}